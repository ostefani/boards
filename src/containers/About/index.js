import React from 'react';
import Page from 'src/containers/MainApp/PageContainer';

export default () => {
    const name='About';
    return (
        <Page>
            <h1>{name}</h1>
        </Page>
    );
};
